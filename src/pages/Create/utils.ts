export const handleSendClient = () => {
	// const addSequence = ITEMS.map((item, idx) => {
	// 	return { ...item, sequence: idx + 1, day: CART_KEY };
	// });
	// const omitCart = omit(itemGroups, CART_KEY);
	// const values = Object.values(omitCart).flat();
	// const payload = flatMap(Object.entries(omitCart), (value, key) => {
	// 	console.log(value);
	// 	return value.map((item) => {
	// 		const target = addSequence.find((o1) => o1.sequence === item);
	// 		const findSequence = values.findIndex((o2) => o2 === item);
	// 		return { ...target, day: key, sequence: findSequence + 1 };
	// 	});
	// });
};

export const handleGetData = () => {
	// const addSequence = ITEMS.map((item, idx) => {
	//     return { ...item, sequence: idx + 1, day: CART_KEY };
	// });
	// const convertGroup = { [CART_KEY]: addSequence.map((item) => item.sequence), 1: [] };
	// setItemGroups(convertGroup);
};
